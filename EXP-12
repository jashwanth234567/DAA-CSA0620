def rob(nums):

    def helper(arr):
        prev=0
        curr=0
        for n in arr:
            prev,curr=curr,max(curr,prev+n)
        return curr

    if len(nums)==1:
        return nums[0]

    return max(helper(nums[:-1]),helper(nums[1:]))

nums=[2,3,2]
print(rob(nums))
